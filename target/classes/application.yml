server:
    port: 8086


spring:
      mvc:
          view:
              prefix : /templates/
spring:
    mvc:
        view:
              suffix : .html

spring:
#    main:
#        banner-mode: "on"
    datasource:
        platform: h2
        driverClassName: org.h2.Driver
        url: jdbc:h2:mem:inMemory

logging:
    level:
        org:
            springframework: ERROR

security:
    basic:
          enabled: false